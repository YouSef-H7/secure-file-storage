# Local Sync Agent Configuration

# Directory to watch for files (recursive)
WATCH_DIR=./sync-folder

# Backend API base URL
API_BASE_URL=http://localhost:3000

# BFF Session cookie (get this after authenticating via web UI)
# Format: connect.sid=<session-id>
SESSION_COOKIE=connect.sid=your-session-id-here

# Optional: polling interval in milliseconds (0 = native fs events)
POLL_INTERVAL=0

# Optional: debounce delay for file changes (ms)
DEBOUNCE_DELAY=500

# Optional: log level (debug, info, warn, error)
LOG_LEVEL=info
